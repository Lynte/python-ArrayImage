from setuptools import setup, find_packages

setup(
    name='ArrayImage',
    version='1.0.0',
    description='pasting table image of string array to input image',
    url='https://github.com/Lynte/python-ArrayImage',
    packages=['ArrayImage'],
    install_requires=['pillow'],
)
